<Window x:Class="TravelSplit.View.TripListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TravelSplit.View"
        xmlns:vm="clr-namespace:TravelSplit.ViewModels"
        mc:Ignorable="d"
        Title="TravelSplit" Height="450" Width="800">
    <Window.DataContext>
        <vm:TripListViewModel />
    </Window.DataContext>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0"
                   Text="✈️ My Trips"
                   FontSize="28"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>
        
        <ListView Grid.Row="1"
                  ItemsSource="{Binding Trips}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Trip Name"
                                    DisplayMemberBinding="{Binding Name}"
                                    Width="200"/>
                    <GridViewColumn Header="Destination"
                                    DisplayMemberBinding="{Binding Destination}"
                                    Width="200"/>
                    <GridViewColumn Header="Start Date" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StartDate, StringFormat=dd/MM/yyyy}"
                                           HorizontalAlignment="Center" 
                                           VerticalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="End Date" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding EndDate, StringFormat=dd/MM/yyyy}"
                                            HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <Button Grid.Row="2"
                Content="+ Add Trip"
                Command="{Binding AddTripCommand}"
                HorizontalAlignment="Right"
                Width="120" Height="34"
                Margin="0,12,0,0"/>
    </Grid>
</Window>
